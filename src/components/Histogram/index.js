import ChartContainer from "../ChartContainer"
import cls from './Histogram.module.css'
const Histogram = (props) => {
    return (
        <ChartContainer className={cls.histogramContainer}/>
    )
}
export default Histogram